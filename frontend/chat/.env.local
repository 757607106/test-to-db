# LangGraph Configuration - Hub-and-Spoke 架构（确定性路由 + 核心 LLM）
NEXT_PUBLIC_API_URL=http://localhost:2024
NEXT_PUBLIC_ASSISTANT_ID=sql_agent
# Do NOT prefix this with "NEXT_PUBLIC_" as we do not want this exposed in the client.
LANGSMITH_API_KEY=

# Backend API Configuration for database connections
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000/api
