# ==========================================
# Chat-to-DB Admin 前端环境配置模板
# ==========================================
# 复制此文件为 .env.local 并根据实际环境修改配置
# cp .env.example .env.local
# ==========================================

# ==========================================
# 统一服务主机配置（核心配置）
# ==========================================
# REACT_APP_SERVICE_HOST 用于配置所有服务的默认主机地址
# 修改此值即可切换所有 API 的访问地址
# 
# 示例：
# - localhost（默认，仅本机访问）
# - 192.168.13.163（局域网 IP 访问）
# - your-server.com（域名访问）
# ==========================================
REACT_APP_SERVICE_HOST=localhost

# ==========================================
# 后端 API 配置
# ==========================================
# REACT_APP_API_URL 优先级高于 SERVICE_HOST
# 如不设置，将自动使用 http://${SERVICE_HOST}:8000/api
# ==========================================
# REACT_APP_API_URL=http://localhost:8000/api

# ==========================================
# Chat 前端 URL 配置
# ==========================================
# REACT_APP_CHAT_URL 优先级高于 SERVICE_HOST
# 如不设置，将自动使用 http://${SERVICE_HOST}:3000
# ==========================================
# REACT_APP_CHAT_URL=http://localhost:3000

# ==========================================
# 混合问答功能配置
# ==========================================
REACT_APP_HYBRID_ENABLED=true

# ==========================================
# 配置优先级说明
# ==========================================
# 1. URL 参数（最高优先级）
#    - ?apiHost=192.168.13.163  覆盖所有服务的主机地址
#    - ?backendUrl=http://...    覆盖后端 API 地址
#    - ?chatUrl=http://...       覆盖 Chat 前端地址
# 
# 2. 完整 URL 环境变量
#    - REACT_APP_API_URL
#    - REACT_APP_CHAT_URL
# 
# 3. SERVICE_HOST 环境变量 + 默认端口
#    - REACT_APP_SERVICE_HOST
# 
# 4. 默认值 localhost
# ==========================================

# ==========================================
# 生产环境配置示例
# ==========================================
# REACT_APP_SERVICE_HOST=your-production-server.com
# REACT_APP_API_URL=https://backend.your-domain.com/api
# REACT_APP_CHAT_URL=https://chat.your-domain.com
# ==========================================
